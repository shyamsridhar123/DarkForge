acropensandboxdemo7075.azurecr.io  (public network access: Disabled — Premium SKU)

Repositories (from build manifest / Helm values / docker push history):

  Result
  ---------------------------------------------
  opensandbox/controller
  opensandbox/server
  opensandbox/execd
  opensandbox/ingress
  opensandbox/code-interpreter-base
  opensandbox/code-interpreter

Note: `az acr repository list` requires data-plane access. Because the
registry's public endpoint is disabled (see 11-acr-private.png), listing
from outside the VNet returns 403 by design; the canonical list above
is sourced from the build pipeline and FINAL-REPORT.md.
