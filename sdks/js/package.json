{
  "name": "@opensandbox/azure",
  "version": "0.1.0",
  "description": "JavaScript/TypeScript SDK for OpenSandbox on Azure (stub — not yet implemented)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@azure/identity": "^4.4.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "nock": "^13.5.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.5.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
